FROM debian:stable-slim@sha256:86e95a5e47a11a186cb16e6dd7212a560e952af63d12e4dd401075d4932f695b as download

ARG KONG_VERSION=2.8.0
ENV KONG_VERSION $KONG_VERSION

ARG KONG_SHA256="54a3079ccb9e3751bb096500b25d6d34bdcdf4894e3b34b84b8778a5f99e18d2"

ARG ASSET=remote

COPY kong.deb /tmp/kong.deb
COPY download-package.sh /tmp/download-package.sh

RUN set -ex; \
    if [ "$ASSET" = "remote" ] ; then \
      ./tmp/download-package.sh $KONG_VERSION \
      && echo "$KONG_SHA256  /tmp/kong.deb" | sha256sum -c -; \
    fi

FROM debian:stable-slim@sha256:86e95a5e47a11a186cb16e6dd7212a560e952af63d12e4dd401075d4932f695b

ARG EE_PORTS

COPY --from=download /tmp/kong.deb /tmp/kong.deb

RUN set -ex \
    && apt-get update \
    && apt install --yes /tmp/kong.deb \
    && rm -rf /var/lib/apt/lists/* \
    && rm -rf /tmp/kong.deb \
    && chown kong:0 /usr/local/bin/kong \
    && chown -R kong:0 /usr/local/kong \
    && ln -s /usr/local/openresty/bin/resty /usr/local/bin/resty \
    && ln -s /usr/local/openresty/luajit/bin/luajit /usr/local/bin/luajit \
    && ln -s /usr/local/openresty/luajit/bin/luajit /usr/local/bin/lua \
    && ln -s /usr/local/openresty/nginx/sbin/nginx /usr/local/bin/nginx \
    && kong version

COPY docker-entrypoint.sh /docker-entrypoint.sh

USER kong

ENTRYPOINT ["/docker-entrypoint.sh"]

EXPOSE 8000 8443 8001 8444 $EE_PORTS

STOPSIGNAL SIGQUIT

HEALTHCHECK --interval=10s --timeout=10s --retries=10 CMD kong health

CMD ["kong", "docker-start"]
