# Specify the DAO to use
database: cassandra

# Enabled plugins, in this order
plugins_enabled:
  - authentication
  - ratelimiting
  - networklog

# Databases configuration
databases_available:
  cassandra:
    properties:
      host: "cassandra"
      port: 9042
      timeout: 1000
      keyspace: kong
      keepalive: 60000
